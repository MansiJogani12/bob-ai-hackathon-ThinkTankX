# YieldSentinel AI — Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data science core
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0

# ML models
xgboost>=2.0,<3.0

# SHAP explainability
shap>=0.45,<1.0

# Model serialisation
joblib>=1.3,<2.0

# HTTP multipart (for CSV upload endpoint)
python-multipart>=0.0.9

# Load .env file
python-dotenv>=1.0,<2.0

# Supabase — Auth & Database
supabase>=2.0,<3.0

# Pydantic v2 is bundled with FastAPI 0.111; no explicit pin needed.
